// Top-level build file where you can add configuration options common to all sub-projects/modules.
// This file should ONLY declare dependencies for the build script itself, NOT for the app.
// We remove the plugins block from here entirely.